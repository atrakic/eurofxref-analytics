name: fx_analytics
version: 1.0
config-version: 2

profile: fx_analytics

model-paths: ["models"]
test-paths: ["tests"]

models:
  fx_analytics:
    staging:
      materialized: view
    intermediate:
      materialized: view
    marts:
      materialized: table
